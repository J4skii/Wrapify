## Packages
framer-motion | Essential for smooth "Wrapped" story transitions and entry animations
lucide-react | Already in base, but emphasizing use for all iconography
date-fns | For formatting dates if needed
clsx | For conditional class names
tailwind-merge | For merging tailwind classes intelligently

## Notes
Tailwind Config: Extended colors for Spotify Green (#1DB954) and Black (#191414).
Font: "Nunito" from Google Fonts.
Auth: Login flow redirects to Spotify OAuth.
API: Requires credentials: "include" for all requests to handle session cookies.
Background: Dark theme by default with gradient accents.
